<% content_for :title, "Editing Job Offer" %>
<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-6 bg-gray-50 dark:bg-gray-900">
  <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-6">Editing Job Offer</h1>
  <%= render "form", job_application: @job_application, job_offer: @job_offer %>
</div>